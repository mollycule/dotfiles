#!/bin/bash
# ~/.config/scripts/rofi-ssh.sh
# Wrapper for rofi's ssh mode

# TODO is this still needed when kitty exists?
#rofi -show ssh -ssh-command 'kitty /home/jhaney/.config/scripts/ssh-wrapper.sh {host}'
